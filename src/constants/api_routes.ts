export default class APIRoutes {
  static base_url = "https://legalbackend.ssgshivalika.com/";
  static auth = `${APIRoutes.base_url}customer/auth/`;
  static me = `${APIRoutes.base_url}customer/me/`;
  static posts = `${APIRoutes.base_url}posts/`;
}
